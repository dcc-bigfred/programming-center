[package]
name = "pc-proto"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "WebSocket JSON protocol for programming-center"
publish = false

[lints]
workspace = true

[lib]
name = "pc_proto"
path = "src/lib.rs"

[dependencies]
pc-core = { path = "../pc-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
