[package]
name = "pc-core"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "Decoder programming domain: bitops and NMRA validation"
publish = false

[lints]
workspace = true

[lib]
name = "pc_core"
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
